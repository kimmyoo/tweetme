Users
    ->reg
    ->login
    ->logout
    ->profile
        ->image
        ->text
        ->follow
    ->feed
        ->user feed ?
        ->user feed + feed of people the user follows

Tweets
    ->creating
        ->text
        ->image
    ->delete
    ->retweet

following/followers
